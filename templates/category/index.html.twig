{% extends 'base.html.twig' %}

{% block body %}

  <div class="container my-5">
    <h1>{{ category.name }}</h1>
    <p> Cette catégorie contient {{ category.products|length }} produit(s).</p>

        <div class="row">
            {% for product in category.products %}
                <div class="card col-md-3 m-2 p-0">
                    <img src="/uploads/{{product.illustration}}" class="card-img-top" alt="image-produit">
                    <div class="card-body">
                        <h5 class="card-title">{{ product.name }}</h5>
                        <p class="card-text">{{ product.description }}</p>
                        <a href="#" class="btn btn-primary">Ajouter au pannier</a>
                    </div>
                </div>
            {% endfor %}
        </div>
  </div>
{% endblock %}
